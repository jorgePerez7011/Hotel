---
// About page - Página "Nosotros" del Hotel Sol
---

<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nosotros - Hotel Sol</title>
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: {
                            50: '#eff6ff',
                            100: '#dbeafe',
                            200: '#bfdbfe',
                            300: '#93c5fd',
                            400: '#60a5fa',
                            500: '#3b82f6',
                            600: '#2563eb',
                            700: '#1d4ed8',
                            800: '#1e40af',
                            900: '#1e3a8a',
                        }
                    }
                }
            }
        }
    </script>
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>

<body class="font-sans">
    <!-- Navigation Header -->
    <header class="bg-white shadow-sm relative z-50">
        <!-- Top Panel -->
        <div class="bg-gray-100 py-2 px-6">
            <div class="container mx-auto flex justify-between items-center">
                <!-- Social Links -->
                <div class="flex items-center space-x-4">
                    <span class="text-sm italic text-gray-600">Síguenos:</span>
                    <div class="flex space-x-2">
                        <a href="#" class="text-gray-600 hover:text-primary-600"><i class="fab fa-instagram"></i></a>
                        <a href="https://www.facebook.com/solhotel1/?locale=es_LA" target="_blank" class="text-gray-600 hover:text-primary-600"><i class="fab fa-facebook"></i></a>
                    </div>
                </div>
                
                <!-- Logo -->
                <div class="text-center">
                    <h1 class="text-2xl font-bold text-gray-800">HOTEL SOL</h1>
                </div>
                
                <!-- Contact Info -->
                <div class="flex items-center space-x-2">
                    <i class="fas fa-phone text-primary-600"></i>
                    <a href="tel:+573001234567" class="text-gray-600 hover:text-primary-600">+57 317 6388290</a>
                </div>
            </div>
        </div>
        
        <!-- Main Navigation -->
        <nav class="py-4 px-6">
            <div class="container mx-auto flex justify-center">
                <ul class="flex space-x-8">
                    <li><a href="/" class="text-gray-700 hover:text-primary-600 transition-colors">Inicio</a></li>
                    <li><a href="/about" class="text-primary-600 font-semibold border-b-2 border-primary-600 pb-1">Nosotros</a></li>
                    <li><a href="/rooms" class="text-gray-700 hover:text-primary-600 transition-colors">Habitaciones</a></li>
                    <li><a href="/contact" class="text-gray-700 hover:text-primary-600 transition-colors">Contacto</a></li>
                    <li><a href="/admin/dashboard" class="text-gray-700 hover:text-primary-600 transition-colors">Panel Admin</a></li>
                </ul>
            </div>
        </nav>
    </header>

    <!-- Hero Section -->
    <section class="relative text-white py-20 overflow-hidden">
        <!-- Background Image -->
        <div class="absolute inset-0 z-0">
            <img src="/Fotos/FrenteHotel.jpg" alt="Hotel Sol" class="w-full h-full object-cover">
            <!-- Overlay oscuro para mejor legibilidad -->
            <div class="absolute inset-0 bg-black/50"></div>
        </div>
        
        <!-- Contenido -->
        <div class="container mx-auto px-6 text-center relative z-10">
            <div class="mb-8">
                <h1 class="text-5xl md:text-6xl font-bold mb-4">Sol Hotel</h1>
                <p class="text-2xl md:text-3xl font-semibold text-primary-200">Confianza y Calidad</p>
            </div>
            <div class="w-24 h-1 bg-yellow-400 mx-auto mb-8"></div>
            <p class="text-xl md:text-2xl max-w-4xl mx-auto leading-relaxed">
                Bienvenidos al Hotel Sol, donde cada estancia se convierte en una experiencia memorable de hospitalidad, confort y excelencia en el servicio.
            </p>
        </div>
    </section>

    <!-- Carrusel de Misión, Visión y Valores -->
    <section class="py-20 bg-white">
        <div class="container mx-auto px-6">
            <div class="text-center mb-12">
                <h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-6">Conoce Nuestros Principios</h2>
                <p class="text-xl text-gray-600 max-w-3xl mx-auto">
                    Descubre los valores y principios que nos guían en nuestra misión de brindar la mejor experiencia hotelera
                </p>
                <div class="w-20 h-1 bg-primary-600 mx-auto mt-6"></div>
            </div>

            <!-- Carrusel Container -->
            <div class="relative max-w-6xl mx-auto">
                <!-- Carrusel Content -->
                <div class="overflow-hidden rounded-3xl shadow-2xl">
                    <div id="carousel-track" class="flex transition-transform duration-700 ease-in-out">
                        
                        <!-- Slide 1: Misión -->
                        <div class="w-full flex-shrink-0">
                            <div class="bg-gradient-to-br from-blue-600 via-blue-700 to-blue-800 text-white p-12 lg:p-16 min-h-[400px] flex items-center relative overflow-hidden">
                                <!-- Elementos decorativos de fondo -->
                                <div class="absolute top-10 right-10 text-blue-300/20 text-8xl transform rotate-12">
                                    <i class="fas fa-bullseye"></i>
                                </div>
                                <div class="absolute bottom-10 left-10 text-blue-300/10 text-6xl transform -rotate-12">
                                    <i class="fas fa-heart"></i>
                                </div>
                                
                                <div class="w-full text-center z-10 relative">
                                    <img src="https://cdn-icons-png.flaticon.com/512/1628/1628441.png" class="w-32 h-32 mx-auto mb-8 animate-pulse" alt="Misión">
                                    <h3 class="text-5xl font-bold mb-8 tracking-wide">MISIÓN</h3>
                                    <div class="w-32 h-1 bg-yellow-400 mx-auto mb-8"></div>
                                    <p class="text-xl md:text-2xl leading-relaxed max-w-4xl mx-auto font-light">
                                        Nuestra pasión es brindar la mejor atención a nuestros huéspedes con hospitalidad, mediante un trato individualizado con amabilidad, compromiso y calidad, desarrollando productos y servicios que garantizan la satisfacción y expectativas del cliente.
                                    </p>
                                </div>
                            </div>
                        </div>

                        <!-- Slide 2: Visión -->
                        <div class="w-full flex-shrink-0">
                            <div class="bg-gradient-to-br from-purple-600 via-purple-700 to-purple-800 text-white p-12 lg:p-16 min-h-[400px] flex items-center relative overflow-hidden">
                                <!-- Elementos decorativos de fondo -->
                                <div class="absolute top-10 left-10 text-purple-300/20 text-8xl transform -rotate-12">
                                    <i class="fas fa-telescope"></i>
                                </div>
                                <div class="absolute bottom-10 right-10 text-purple-300/10 text-6xl transform rotate-12">
                                    <i class="fas fa-star"></i>
                                </div>
                                
                                <div class="w-full text-center z-10 relative">
                                    <img src="https://cdn-icons-png.flaticon.com/512/9640/9640649.png" class="w-32 h-32 mx-auto mb-8 animate-pulse" alt="Visión">
                                    <h3 class="text-5xl font-bold mb-8 tracking-wide">VISIÓN</h3>
                                    <div class="w-32 h-1 bg-yellow-400 mx-auto mb-8"></div>
                                    <p class="text-xl md:text-2xl leading-relaxed max-w-4xl mx-auto font-light">
                                        Trabajamos para ser reconocidos, tanto por nuestras modernas instalaciones como por el servicio familiar y cercano que nos diferencia de la competencia.
                                    </p>
                                </div>
                            </div>
                        </div>

                        <!-- Slide 3: Valores -->
                        <div class="w-full flex-shrink-0">
                            <div class="bg-gradient-to-br from-green-600 via-green-700 to-green-800 text-white p-12 lg:p-16 min-h-[600px] flex items-center relative overflow-hidden">
                                <!-- Elementos decorativos de fondo -->
                                <div class="absolute top-10 right-10 text-green-300/20 text-8xl transform rotate-12">
                                    <i class="fas fa-gem"></i>
                                </div>
                                <div class="absolute bottom-10 left-10 text-green-300/10 text-6xl transform -rotate-12">
                                    <i class="fas fa-handshake"></i>
                                </div>
                                
                                <div class="w-full text-center z-10 relative">
                                    <img src="https://cdn-icons-png.flaticon.com/512/11282/11282325.png" class="w-32 h-32 mx-auto mb-8 animate-pulse" alt="Valores">
                                    <h3 class="text-5xl font-bold mb-8 tracking-wide">VALORES</h3>
                                    <div class="w-32 h-1 bg-yellow-400 mx-auto mb-8"></div>
                                    <p class="text-lg mb-12 max-w-3xl mx-auto font-light">
                                        Los valores de Sol Hotel están interrelacionados y se complementan mutuamente. Estos son la base de nuestro trabajo y nuestra mejora diaria.
                                    </p>
                                </div>
                                
                                <div class="grid grid-cols-1 md:grid-cols-2 gap-6 max-w-5xl mx-auto z-10 relative">
                                    <div class="bg-white/20 backdrop-blur-sm rounded-xl p-6 text-center hover:bg-white/30 transition-all duration-300 hover:scale-105">
                                        <div class="text-4xl mb-4">✨</div>
                                        <h4 class="font-bold text-xl mb-3">CALIDAD</h4>
                                        <p class="text-sm leading-relaxed">Compromisos de calidad garantizando la excelencia en cada detalle de la experiencia de nuestros huéspedes.</p>
                                    </div>
                                    
                                    <div class="bg-white/20 backdrop-blur-sm rounded-xl p-6 text-center hover:bg-white/30 transition-all duration-300 hover:scale-105">
                                        <div class="text-4xl mb-4">🤝</div>
                                        <h4 class="font-bold text-xl mb-3">TRABAJO EN EQUIPO</h4>
                                        <p class="text-sm leading-relaxed">Un equipo que trabaja conjuntamente para lograr la satisfacción del cliente y la de uno mismo.</p>
                                    </div>
                                    
                                    <div class="bg-white/20 backdrop-blur-sm rounded-xl p-6 text-center hover:bg-white/30 transition-all duration-300 hover:scale-105">
                                        <div class="text-4xl mb-4">🛡️</div>
                                        <h4 class="font-bold text-xl mb-3">CONFIANZA</h4>
                                        <p class="text-sm leading-relaxed">Servicios e instalaciones de calidad que mejoramos y modernizamos año tras año.</p>
                                    </div>
                                    
                                    <div class="bg-white/20 backdrop-blur-sm rounded-xl p-6 text-center hover:bg-white/30 transition-all duration-300 hover:scale-105">
                                        <div class="text-4xl mb-4">⭐</div>
                                        <h4 class="font-bold text-xl mb-3">SERVICIO EXCEPCIONAL</h4>
                                        <p class="text-sm leading-relaxed">El mejor nivel de servicio en cada interacción con nuestros huéspedes.</p>
                                    </div>
                                </div>
                            </div>
                        </div>

                     
                    </div>
                </div>

                <!-- Botones de Navegación -->
                <button id="prevBtn" class="absolute left-6 top-1/2 transform -translate-y-1/2 bg-white/90 hover:bg-white text-gray-800 p-4 rounded-full shadow-xl transition-all duration-300 hover:scale-110 z-20 opacity-80 hover:opacity-100">
                    <i class="fas fa-chevron-left text-xl"></i>
                </button>
                
                <button id="nextBtn" class="absolute right-6 top-1/2 transform -translate-y-1/2 bg-white/90 hover:bg-white text-gray-800 p-4 rounded-full shadow-xl transition-all duration-300 hover:scale-110 z-20 opacity-80 hover:opacity-100">
                    <i class="fas fa-chevron-right text-xl"></i>
                </button>

                <!-- Indicadores de Slide -->
                <div class="absolute bottom-6 left-1/2 transform -translate-x-1/2 flex space-x-3 z-20">
                    <button class="slide-indicator w-4 h-4 rounded-full bg-white/50 hover:bg-white transition-all duration-300 active-slide" data-slide="0"></button>
                    <button class="slide-indicator w-4 h-4 rounded-full bg-white/50 hover:bg-white transition-all duration-300" data-slide="1"></button>
                    <button class="slide-indicator w-4 h-4 rounded-full bg-white/50 hover:bg-white transition-all duration-300" data-slide="2"></button>
                </div>

                <!-- Barra de Progreso -->
                <div class="absolute bottom-0 left-0 w-full h-1 bg-black/20 z-10">
                    <div id="progress-bar" class="h-full bg-gradient-to-r from-yellow-400 to-yellow-500 transition-all duration-700 ease-out" style="width: 33.33%;"></div>
                </div>
            </div>
        </div>
    </section>

    <!-- Instalaciones y Servicios -->
    <section class="py-20 bg-gray-50">
        <div class="container mx-auto px-6">
            <div class="text-center mb-16">
                <h2 class="text-4xl font-bold text-gray-900 mb-4">Nuestras Instalaciones</h2>
                <p class="text-xl text-gray-600 max-w-2xl mx-auto">
                    Modernas instalaciones pensadas para brindar el máximo confort y satisfacción a nuestros huéspedes
                </p>
                <div class="w-20 h-1 bg-primary-600 mx-auto mt-6"></div>
            </div>

            <div class="grid md:grid-cols-3 gap-8 mb-16">
                <!-- Habitaciones Confortables -->
                <div class="text-center bg-white rounded-xl p-8 shadow-lg hover:shadow-xl transition-shadow">
                    <div class="text-5xl text-primary-600 mb-6">🛏️</div>
                    <h3 class="text-xl font-bold text-gray-900 mb-4">Habitaciones Confortables</h3>
                    <p class="text-gray-600 leading-relaxed">
                        20 habitaciones equipadas con todas las comodidades modernas para garantizar un descanso reparador.
                    </p>
                </div>

                <!-- Piscina y Spa -->
                <div class="text-center bg-white rounded-xl p-8 shadow-lg hover:shadow-xl transition-shadow">
                    <div class="text-5xl text-primary-600 mb-6">☂️</div>
                    <h3 class="text-xl font-bold text-gray-900 mb-4">Sala Comun</h3>
                    <p class="text-gray-600 leading-relaxed">
                        Instalaciones de relajación y bienestar para que disfrutes de momentos de tranquilidad y renovación.
                    </p>
                </div>

                <!-- Servicios Personalizados -->
                <div class="text-center bg-white rounded-xl p-8 shadow-lg hover:shadow-xl transition-shadow">
                    <div class="text-5xl text-primary-600 mb-6">🎯</div>
                    <h3 class="text-xl font-bold text-gray-900 mb-4">Atención Personalizada</h3>
                    <p class="text-gray-600 leading-relaxed">
                        Servicio familiar y cercano que nos diferencia, con atención individualizada para cada huésped.
                    </p>
                </div>
            </div>
        </div>
    </section>

    <!-- Horarios y Contacto -->
    <section class="py-20 bg-primary-600 text-white">
        <div class="container mx-auto px-6">
            <div class="text-center mb-16">
                <h2 class="text-4xl font-bold mb-4">Información de Contacto</h2>
                <div class="w-20 h-1 bg-yellow-400 mx-auto"></div>
            </div>

            <div class="grid md:grid-cols-3 gap-12 text-center">
                <!-- Horarios -->
                <div class="bg-white/10 rounded-xl p-8">
                    <div class="text-4xl mb-6">🕐</div>
                    <h3 class="text-2xl font-bold mb-6">Horarios de Atención</h3>
                    <div class="space-y-4">
                        <div>
                            <p class="font-semibold text-lg">Entre semana:</p>
                            <p class="text-primary-200 text-xl">8:00 – 20:00</p>
                        </div>
                        <div>
                            <p class="font-semibold text-lg">Fin de semana:</p>
                            <p class="text-primary-200 text-xl">9:00 – 18:00</p>
                        </div>
                    </div>
                </div>

                <!-- Ubicación -->
                <div class="bg-white/10 rounded-xl p-8">
                    <div class="text-4xl mb-6">📍</div>
                    <h3 class="text-2xl font-bold mb-6">Nuestra Ubicación</h3>
                    <div class="space-y-4">
                        <p class="text-primary-100">
                            Cl. 19 #8-56<br>
                            San Martín, Cesar<br>
                            Colombia
                        </p>
                    </div>
                </div>

                <!-- Contacto -->
                <div class="bg-white/10 rounded-xl p-8">
                    <div class="text-4xl mb-6">📞</div>
                    <h3 class="text-2xl font-bold mb-6">Contáctanos</h3>
                    <div class="space-y-4">
                        <div>
                            <p class="font-semibold">Teléfono:</p>
                            <div class="space-y-2">
                                <a href="tel:+573001234567" class="text-primary-200 hover:text-white text-lg block">
                                   +57 317 6388290 - 323 3928158
                                </a>
                                <a href="tel:+573233928158" class="text-primary-200 hover:text-white text-lg block">
                                   +57 323 3928158
                                </a>
                            </div>
                        </div>
                        <div>
                            <p class="font-semibold">Email:</p>
                            <a href="mailto:solhotel.recepcion@gmail.com" class="text-primary-200 hover:text-white">
                                solhotel.recepcion@gmail.com
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Call to Action -->
            <div class="text-center mt-16">
                <a href="/" class="inline-block bg-yellow-400 text-primary-900 font-bold text-xl px-12 py-4 rounded-full hover:bg-yellow-300 transition-colors shadow-lg">
                    🏨 Hacer una Reserva
                </a>
            </div>
        </div>
    </section>

  

    <style>
        /* Estilos del Carrusel */
        .slide-indicator.active-slide {
            background-color: white !important;
            transform: scale(1.2);
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.5);
        }

        /* Animaciones suaves */
        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-10px) rotate(5deg); }
        }

        .fas {
            animation: float 4s ease-in-out infinite;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .carousel-slide h3 {
                font-size: 2.5rem !important;
            }
            
            .carousel-slide p {
                font-size: 1.1rem !important;
            }
        }
    </style>

    <script>
        // Variables del carrusel
        let currentSlide = 0;
        const totalSlides = 3;

        // Función para ir al siguiente slide
        function nextSlide() {
            currentSlide = (currentSlide + 1) % totalSlides;
            updateCarousel();
        }

        // Función para ir al slide anterior
        function prevSlide() {
            currentSlide = currentSlide === 0 ? totalSlides - 1 : currentSlide - 1;
            updateCarousel();
        }

        // Función para ir a un slide específico
        function goToSlide(slideIndex) {
            if (slideIndex >= 0 && slideIndex < totalSlides) {
                currentSlide = slideIndex;
                updateCarousel();
            }
        }

        // Función para actualizar la posición del carrusel
        function updateCarousel() {
            const track = document.getElementById('carousel-track');
            const indicators = document.querySelectorAll('.slide-indicator');
            const progressBar = document.getElementById('progress-bar');
            
            // Mover el carrusel
            if (track) {
                track.style.transform = `translateX(-${currentSlide * 100}%)`;
            }
            
            // Actualizar indicadores
            indicators.forEach((indicator, index) => {
                if (index === currentSlide) {
                    indicator.classList.add('active-slide');
                } else {
                    indicator.classList.remove('active-slide');
                }
            });
            
            // Actualizar barra de progreso
            if (progressBar) {
                const progressWidth = ((currentSlide + 1) / totalSlides) * 100;
                progressBar.style.width = progressWidth + '%';
            }
        }

        // Inicializar el carrusel cuando se carga la página
        document.addEventListener('DOMContentLoaded', function() {
            // Configurar event listeners para los botones
            const prevBtn = document.getElementById('prevBtn');
            const nextBtn = document.getElementById('nextBtn');
            const indicators = document.querySelectorAll('.slide-indicator');
            const carouselContainer = document.querySelector('.relative.max-w-6xl');
            
            // Botones de navegación
            if (prevBtn) {
                prevBtn.addEventListener('click', function(e) {
                    e.preventDefault();
                    prevSlide();
                });
            }
            
            if (nextBtn) {
                nextBtn.addEventListener('click', function(e) {
                    e.preventDefault();
                    nextSlide();
                });
            }
            
            // Indicadores
            indicators.forEach((indicator, index) => {
                indicator.addEventListener('click', function(e) {
                    e.preventDefault();
                    goToSlide(index);
                });
            });
            
            // Pausar auto-slide cuando el mouse está sobre el carrusel
            if (carouselContainer) {
                // Soporte para gestos táctiles (móvil)
                let startX = 0;
                let endX = 0;
                
                carouselContainer.addEventListener('touchstart', function(e) {
                    startX = e.touches[0].clientX;
                });
                
                carouselContainer.addEventListener('touchend', function(e) {
                    endX = e.changedTouches[0].clientX;
                    handleSwipe();
                });
                
                function handleSwipe() {
                    const swipeThreshold = 50;
                    const diff = startX - endX;
                    
                    if (Math.abs(diff) > swipeThreshold) {
                        if (diff > 0) {
                            nextSlide(); // Swipe left - next slide
                        } else {
                            prevSlide(); // Swipe right - previous slide
                        }
                    }
                }
            }
            
            // Soporte para teclado
            document.addEventListener('keydown', function(e) {
                if (e.key === 'ArrowLeft') {
                    prevSlide();
                } else if (e.key === 'ArrowRight') {
                    nextSlide();
                }
            });
            
            // Inicializar el carrusel
            updateCarousel();
        });
    </script>
</body>
</html>